<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Location (for capturing point coordinates) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Camera (now essential for AR features) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Camera hardware feature -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <!-- ARCore requirements -->
    <uses-feature
        android:name="android.hardware.camera.ar"
        android:required="false" />
    <uses-feature
        android:glEsVersion="0x00030000"
        android:required="true" />

    <!-- Notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Network (for map tiles, TCP GNSS, etc.) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Foreground service for continuous location -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Wi-Fi and multicast permissions required for mDNS (JmDNS) discovery -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!--
        BLUETOOTH_CONNECT is required on Android 12+ (API 31) to read bonded device
        names/addresses — used only in LogZip diagnostic reporting. The code already
        catches SecurityException gracefully so the app runs fine without it, but
        declaring it here prevents the SecurityException entirely when the user grants it.
        maxSdkVersion is not set so it is requested on all future API levels too.
    -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        android:minSdkVersion="31" />

    <application
        android:name=".SurveyingApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SurveyingApp"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true">

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${GOOGLE_MAPS_API_KEY}"/>

git        <!-- ARCore Geospatial API Key (same Google Cloud project key as Maps) -->
        <meta-data
            android:name="com.google.android.ar.API_KEY"
            android:value="${GOOGLE_MAPS_API_KEY}"/>

        <!-- ARCore metadata - 'optional' allows app to run on non-AR devices -->
        <meta-data
            android:name="com.google.ar.core"
            android:value="optional" />

        <activity
            android:name=".ui.viewpoints.CoordinatesActivity"
            android:exported="true"
            android:theme="@style/Theme.SurveyingApp.NoActionBar" />

        <activity
            android:name=".ui.filepicker.FilePickerActivity"
            android:exported="false"
            android:theme="@style/Theme.SurveyingApp.NoActionBar" />

        <activity
            android:name=".ui.models.ModelViewerActivity"
            android:exported="false"
            android:theme="@style/Theme.SurveyingApp.NoActionBar" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize"
            android:theme="@style/Theme.SurveyingApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Invisible activity used purely to render a GLB model and capture a thumbnail.
            Uses a transparent theme and no-animation flags so the user sees nothing.
        -->
        <activity
            android:name=".ui.models.ThumbnailCaptureActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@style/Theme.SurveyingApp.ThumbnailCapture" />

        <service
            android:name=".service.LocationService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- FileProvider for sharing exported files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>
</manifest>
