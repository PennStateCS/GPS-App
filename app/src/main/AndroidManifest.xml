<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        [Storage Permissions Bible]

        READ_EXTERNAL_STORAGE:
        - Usable till API 32
        - From API 33+, replaced by READ_MEDIA_* permissions

        WRITE_EXTERNAL_STORAGE:
        - Usable till API 28
        - In API 29, deprecated due to Scoped Storage enforcement, but
        can still be used if requestLegacyExternalStorage="true" is set in the
        application tag (not recommended)
        - From API 30+, deprecated due to Scoped Storage, instead use MANAGE_EXTERNAL_STORAGE

        MANAGE_EXTERNAL_STORAGE:
        - Introduced in API 30
        - Grants broad access to external storage
        - Requires special user permission via system settings
        - NOT ALLOWED on the Google Play Store
    -->

    <!-- Image permissions for API 33+-->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- Storage permissions for API 30+ -->
    <!-- This will not fly with Google Play -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- Legacy: READ used up to API 32 (Android 12L) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Legacy: WRITE used up to API 29 (Android 10) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" android:requestLegacyExternalStorage="true" />




    <!-- Location (for capturing point coordinates) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Camera (now essential for AR features) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Camera hardware feature -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <!-- ARCore requirements -->
    <uses-feature
        android:name="android.hardware.camera.ar"
        android:required="false" />
    <uses-feature
        android:glEsVersion="0x00030000"
        android:required="true" />

    <!-- Notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Network (for map tiles, TCP GNSS, etc.) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Foreground service for continuous location -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Wi-Fi and multicast permissions required for mDNS (JmDNS) discovery -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <application
        android:name=".SurveyingApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SurveyingApp"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true">

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${GOOGLE_MAPS_API_KEY}"/>

        <!-- ARCore metadata - 'optional' allows app to run on non-AR devices -->
        <meta-data
            android:name="com.google.ar.core"
            android:value="optional" />

        <activity
            android:name=".ui.viewpoints.CoordinatesActivity"
            android:exported="true"
            android:theme="@style/Theme.SurveyingApp.NoActionBar" />

        <activity
            android:name=".ui.filepicker.FilePickerActivity"
            android:exported="false"
            android:theme="@style/Theme.SurveyingApp.NoActionBar" />

        <activity
            android:name=".ui.models.ModelViewerActivity"
            android:exported="false"
            android:theme="@style/Theme.SurveyingApp.NoActionBar" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SurveyingApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.LocationService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- FileProvider for sharing exported files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>
</manifest>
