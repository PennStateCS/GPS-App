package com.example.surveyingapp.data

// Backward compatibility: legacy code references Point. Coordinate is the new name.
// Typealias keeps source compatibility without duplicating the entity.

typealias Point = Coordinate

