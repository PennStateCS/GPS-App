package app.surrealar.di

import org.junit.Ignore
import org.junit.Test

/**
 * Tests for the GNSS dependency graph singleton.
 *
 * These tests were written against a `GnssGraph` class that has since been removed or renamed.
 * They are @Ignore'd until the DI graph tests are updated to match the current codebase.
 */
@Ignore("GnssGraph class no longer exists; tests need updating to match current DI graph")
class GnssGraphTest {
    @Test
    fun placeholder() {
        // Intentionally empty — see class-level @Ignore.
    }
}
