# Built outputs
*.iml
/build
/captures
app.zip
app/src.zip

# Gradle
.gradle/
.gradle-tmp/
/local.properties

# Android generated
.externalNativeBuild
.cxx

# IDE
/.idea/
.DS_Store

# Kotlin build cache
.kotlin/

# Secrets & keys — never commit these
local.properties
google-services.json
*.keystore
*.jks
*.p12
*.pfx
.env
.env.*
!.env.example
# Exception: the SHARED DEBUG keystore is intentionally committed so every developer
# signs local debug builds with the same SHA-1 (required for the Google Maps API key
# restriction). This is a debug-only key — never use it for release signing.
!keystores/shared-debug.keystore

# Release artifacts
*.apk
*.aab
*.ap_

# Logs
*.log

# AI coding guide — local only
AGENTS.md

# Claude Code — local only
.claude

# Extracted Filament AAR artifacts — managed by Gradle, not source
tmp_filament/

